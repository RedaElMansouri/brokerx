<form id="orderForm">
    <div class="grid-2 mb-2">
        <label>Symbole:<br/><input id="symbol" required value="AAPL" class="input"/></label>
        <label>Type:<br/>
            <select id="order_type" class="select">
        <option value="market">market</option>
        <option value="limit">limit</option>
        </select>
    </label>
    </div>

    <div class="grid-2 mb-2">
        <label>Direction:<br/>
            <select id="direction" class="select">
        <option value="buy">buy</option>
        <option value="sell">sell</option>
        </select>
    </label>
        <label>Quantité:<br/><input id="quantity" type="number" value="1" min="1" required class="input"/></label>
    </div>

    <div class="mb-2">
        <label>Prix (pour limite):<br/><input id="price" type="number" step="0.01" class="input"/></label>
    </div>

    <div class="mb-2">
    <label>Client Order ID (idempotence, optionnel):<br/>
            <div class="row">
                <input id="client_order_id" placeholder="auto si vide" class="input flex-1"/>
                <button id="genClientOrderId" type="button" class="btn btn-slate">Générer</button>
        </div>
    </label>
        <div class="hint">Laisser vide pour générer un identifiant unique automatiquement. Réutiliser le même identifiant lors d'une nouvelle tentative évite les doublons.</div>
    </div>

    <div class="row">
        <button type="submit" class="cta-button">Envoyer l'ordre</button>
        <span id="orderFeedback" class="text-error"></span>
    </div>
</form>
