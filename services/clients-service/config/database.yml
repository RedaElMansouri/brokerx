# frozen_string_literal: true

# Clients Service Database Configuration
# This service has its own isolated PostgreSQL database

default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  host: <%= ENV.fetch("DATABASE_HOST", "localhost") %>
  username: <%= ENV.fetch("DATABASE_USERNAME", "postgres") %>
  password: <%= ENV.fetch("DATABASE_PASSWORD", "postgres") %>

development:
  <<: *default
  database: clients_service_development
  port: <%= ENV.fetch("DATABASE_PORT", 5433) %>

test:
  <<: *default
  database: clients_service_test
  port: <%= ENV.fetch("DATABASE_PORT", 5433) %>

production:
  <<: *default
  database: clients_service_production
  port: <%= ENV.fetch("DATABASE_PORT", 5432) %>
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 10 } %>
